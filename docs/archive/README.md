# Archive — historical, not authoritative

Nothing here describes the current system as truth. These docs are kept for history and provenance only. **For current behaviour, use the active docs under [`../`](../README.md).**

What's in here:

- **Superseded docs** — replaced by a current doc. Each `superseded-*` file names what replaced it:
  - `superseded-student-cycle-0-9-scale.md` → replaced by [`../student/lifecycle.md`](../student/lifecycle.md) (this one uses the old 0–9 band scale, pre 4–9 migration).
  - `superseded-phase5-exam-config-input-questions.md` → replaced by [`../platform/exam-engine/phase5-exam-config-input.md`](../platform/exam-engine/phase5-exam-config-input.md) (answered version).
  - `exam-engine-superseded-v1/` → the v1 (“paul”) exam-engine spec, superseded by [`../platform/exam-engine/`](../platform/exam-engine/README.md).
- **Completed / point-in-time** — finished plans, audits, verification reports, release-readiness snapshots, gap reports.
- **`old/`** — the previous catch-all of older plans, checklists, migrations, and analyses.
- **`daily-reports/`** — dated standup reports.
- **`_assets/`** — non-documentation files quarantined out of the doc tree: source-code copies (`backend-code/`, `exam-engine/*.cjs`, `refs/*.jsx`), seed/test data (`data/`), a decision-record PDF, and an audio recording. These are not maintained; the real source/data lives in the codebase.
